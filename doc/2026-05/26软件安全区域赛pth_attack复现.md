#  26软件安全区域赛pth_attack复现  
咕咕咕
                    咕咕咕  正在思考ing   2026-05-09 01:25  
  
## 前言  
  
比赛的时候提取完winrm流量解码完就没进展了，当时就觉得解码后的流量怎么这么少😳，然后比赛之后复现发现还真是，复现的时候，我是真的绷不住了，一波三折，快给我整不会了😭。还得是ai大人伸出援手（屋檐了），让我成功复现😄  
## 正文  
  
![](https://mmbiz.qpic.cn/mmbiz_png/g673ce4c7rmb2uuI0LLdxb2SbBTuOYQVv12MxMicZvIrqcB7YHxn3tTg4mfic5l7NUdsNkfSThuniaJww6LxBOlu1Lp6nHAUN28pJ7lFsucuU8/640?wx_fmt=png&from=appmsg "")  
  
首先是题目原件是两个流量包，题目提示是这两个流量包是内网渗透流量 打开第一个![](https://mmbiz.qpic.cn/mmbiz_png/g673ce4c7rkZukx9fj5Zs6tnBNIhEImFRF64UV9ib157RShVMXb9WpyWgMUyu3upsaGzZCYnTWU4ONuibkFh274IJicIMia4oqKDPcIMzcfnacg/640?wx_fmt=png&from=appmsg "")  
![](https://mmbiz.qpic.cn/sz_mmbiz_png/g673ce4c7rmaj7bGK1gpe3QuicCls4BpMwNqmWTvpcI7jlicCcsb3SukFxImBgD5FlIdW0n6BTibNe80goWXH4UET4KoBfoUBDjvdOHt9icIcD4/640?wx_fmt=png&from=appmsg "")  
![](https://mmbiz.qpic.cn/mmbiz_png/g673ce4c7rnyFB5ibASwDPzZTtv8Cq1qQdh6g4Zcc1NULjNu6tZsrUg8ia0XwswTibTRDtBppagZyhic4u8zPGSsbGYlB9bN5bGrc9xHxFeoPDg/640?wx_fmt=png&from=appmsg "")  
主要是smb流量和winrm流量![](https://mmbiz.qpic.cn/mmbiz_png/g673ce4c7rkeTnTro0j87GIdo7x793ibBZibxiaoCwfgBrav6DNpPlJQicwrUibLN9tibYXb4lrZasnZOLP1Hr9QuHmJwwRQs7Kg8AJjTrpxfp6Ok/640?wx_fmt=png&from=appmsg "")  
看到这里有smb连接成功后进行传输的报文，想法是拿到NTLMv2 hash，然后爆破NTLMSSP的值 然后使用github上的项目，使用项目中的脚本 `python NTLMRawUnHide.py -i 1-pth.pcapng 直接就能够得到NTLMv2的hash，然后经过分析只有最下面两个是有用的![](https://mmbiz.qpic.cn/mmbiz_png/g673ce4c7rlyJcHQx83ibK9ddQFp6kkylNoCf82hXib7Nd9ziaicK3jdNoZRvMY1ia13Sg4BCDxMXsReoA3W16XambTMtibUs6lnldsvibLTzv0pIQ/640?wx_fmt=png&from=appmsg "")  
因为流量中显示只有两次连接成功![](https://mmbiz.qpic.cn/sz_mmbiz_png/g673ce4c7rmIt45W9H7XibwlH3KasC1tFPknw4j7Qko4ECjJNVdHLCRJz4f2xXRCYNoK7ibIW0zrjOZPmbSiaNqkRLPOshniaTcKFiapYqHiaOWBA/640?wx_fmt=png&from=appmsg "")  
![](https://mmbiz.qpic.cn/mmbiz_png/g673ce4c7rkREdnjW15AWPnVooxKRzKs1fIAEMuypId6NDkQhUlhhp3FnZWjPOF8hoHY8H0REHkOHkgAPpFbH6WznyO4Q6gJIMohaspLwCA/640?wx_fmt=png&from=appmsg "")  
  
  
然后就是想直接使用hashcat进行hash爆破了hashcat -m 5600 -a 0 hash.txt rockyou.txt  
然后很难过的事情发生了，kail的这个默认字典文件rockyou.txt爆破失败了 那么不得不进一步分析了，然后前面不是发现还有winrm流量吗，了解到winrm流量，NTLMRawUnHide.py这个脚本是提取不到的，那么是时候手搓了 形式是这样的$NETNTLMv2$USERNAME+domain$server challenge$NTProofstr$blob  
![](https://mmbiz.qpic.cn/sz_mmbiz_png/g673ce4c7rlJYKrsrWEOWawMLjRoz6joVepicgzv4yb1riceM65r9OJia9hYVZxDvVdb4XF5orcH1jO0ZgtTwSQ8P7SBia8Q9UqMBrU9U8ibVsGg/640?wx_fmt=png&from=appmsg "")  
所需的内容从这样一对数据包中找，前一个 是ntlm_challeng,后一个是ntlm_auth流量![](https://mmbiz.qpic.cn/mmbiz_png/g673ce4c7rkgksAhhebGHfe2ia0z95uMKGiblicmq3IXd2MqIBKmavcx2p2KXrmkgsTXeiabq7ERAIibsH8sTxfX2X6c1SQyXfs6njWP6f5yvMOg/640?wx_fmt=png&from=appmsg "")  
![](https://mmbiz.qpic.cn/mmbiz_png/g673ce4c7rkprC2picnqE1TpOv0bv0fFgQicxv5T0P9FUegO9Aoekk5SZkiaxfOBPx8Kj732zf1jo3OnXQVbv7Tg5YapS0zic3ibGkmbTGelXMrw/640?wx_fmt=png&from=appmsg "")  
![](https://mmbiz.qpic.cn/sz_mmbiz_png/g673ce4c7rlPUwv1cjxibJpKptDPVshm9kK3YPKhUGRgJD3ooQJBicF4qVKjMCK4oZph638LgqujnfBgBxdvHKnnP3EzRDescMtmxEZZvWFuo/640?wx_fmt=png&from=appmsg "")  
$NETNTLMv2$ADMINISTRATORpc$cd0a6722277096c9$3fa965e4d9af9a92bde5cefcdd309acb$  
然后使用john爆破![](https://mmbiz.qpic.cn/sz_mmbiz_png/g673ce4c7rmMWBmQyPDCY2dYgjibbEHFZvHFYo7adficm6zZ2FLa3HLCfpQJSVl05mVf5ibTp7GJuUcXtdiaXylbBlIsZCQG8m5ANS9R3MY2mLo/640?wx_fmt=png&from=appmsg "")  
拿到了密码pass@word1  
然后就可以将winrm流量包的内容提取出来了（后面的经历我已经想流泪了） 最开始我是想直接用NATe直接提取的![](https://mmbiz.qpic.cn/sz_mmbiz_png/g673ce4c7rmEgtVzQyia5Xkd2mzJodacMRP9K2YzC7jXfLkVBCtFYC7OBpibatvhJypXXBYOPxqXDl7lZDj0OhUgjapmCe7D1xgaNA6uABDBk/640?wx_fmt=png&from=appmsg "")  
![](https://mmbiz.qpic.cn/sz_mmbiz_png/g673ce4c7rniaficWEeXw6tDD5vlS7MF3WgtrzQndxqNWo0snYFARxNRbSmASibHsYcsicicRo7eWO51PlzzHfgW7vnGf6MyNnt48afib61BoQsrE/640?wx_fmt=png&from=appmsg "")  
但是得到txt文件的内容不全，然后不得不去请出ai大人（当然可能是我不会用 NETA，有不对的地方请大佬指正）  
```
#!/usr/bin/env python3# -*- coding: utf-8 -*-# PYTHON_ARGCOMPLETE_OK# Copyright: (c) 2020 Jordan Borean (@jborean93) <jborean93@gmail.com># MIT License (see LICENSE or https://opensource.org/licenses/MIT)# Fork / modifications by Haoxi Tan (haoxi.tan@gmail.com)# MIT License (see LICENSE or https://opensource.org/licenses/MIT)# Further modified to use scapy instead of pyshark (no tshark dependency)"""Script that can read a Wireshark capture .pcap/.pcapng for a WinRM exchange and decrypt the messages.Currently only supports exchanges that were authenticated with NTLM."""from __future__ import (absolute_import, division, print_function)__metaclass__ = typeimport argparseimport base64import collectionsimport hashlibimport hmacimport osimport binasciiimport sysimport structimport xml.dom.minidomfrom scapy.all import rdpcap, TCP, IP, Rawfrom Crypto.Hash import MD4from cryptography.hazmat.primitives.ciphers import (    algorithms,    Cipher,)from cryptography.hazmat.backends import (    default_backend,)try:    import argcompleteexcept ImportError:    argcomplete = Noneclass SecurityContext:    def __init__(self, port, nt_hash):        self.port = port        self.tokens = []        self.nt_hash = nt_hash        self.complete = False        self.key_exch = False        self.session_key = None        self.sign_key_initiate = None        self.sign_key_accept = None        self.seal_handle_initiate = None        self.seal_handle_accept = None        self.__initiate_seq_no = 0        self.__accept_seq_no = 0    @property    def _initiate_seq_no(self):        val = self.__initiate_seq_no        self.__initiate_seq_no += 1        return val    @property    def _accept_seq_no(self):        val = self.__accept_seq_no        self.__accept_seq_no += 1        return val    def add_token(self, token):        self.tokens.append(token)        if token.startswith(b"NTLMSSP\x00\x03"):            # Extract the info required to build the session key            nt_challenge = self._get_auth_field(20, token)            b_domain = self._get_auth_field(28, token) or b""            b_username = self._get_auth_field(36, token) or b""            encrypted_random_session_key = self._get_auth_field(52, token)            flags = struct.unpack("<I", token[60:64])[0]            encoding = 'utf-16-le'if flags & 0x00000001 else'windows-1252'            domain = b_domain.decode(encoding)            username = b_username.decode(encoding)            # Derive the session key            nt_proof_str = nt_challenge[:16]            response_key_nt = hmac_md5(self.nt_hash, (username.upper() + domain).encode('utf-16-le'))            key_exchange_key = hmac_md5(response_key_nt, nt_proof_str)            self.key_exch = bool(flags & 0x40000000)            if self.key_exch and (flags & (0x00000020 | 0x00000010)):                self.session_key = rc4k(key_exchange_key, encrypted_random_session_key)            else:                self.session_key = key_exchange_key            # Derive the signing and sealing keys            self.sign_key_initiate = signkey(self.session_key, 'initiate')            self.sign_key_accept = signkey(self.session_key, 'accept')            self.seal_handle_initiate = rc4init(sealkey(self.session_key, 'initiate'))            self.seal_handle_accept = rc4init(sealkey(self.session_key, 'accept'))            self.complete = True    def unwrap_initiate(self, data):        print('unwrap_initiate', file=sys.stderr)        return self._unwrap(self.seal_handle_initiate, self.sign_key_initiate, self._initiate_seq_no, data)    def unwrap_accept(self, data):        print('unwrap_accept', file=sys.stderr)        return self._unwrap(self.seal_handle_accept, self.sign_key_accept, self._accept_seq_no, data)    def _unwrap(self, handle, sign_key, seq_no, data):        header = data[4:20]        enc_data = data[20:]        dec_data = handle.update(enc_data)        b_seq_num = struct.pack("<I", seq_no)        checksum = hmac_md5(sign_key, b_seq_num + dec_data)[:8]        if self.key_exch:            checksum = handle.update(checksum)        actual_header = b"\x01\x00\x00\x00" + checksum + b_seq_num        if header != actual_header:            print("Signature verification failed", file=sys.stderr)        return dec_data    def _get_auth_field(self, offset, token):        field_len = struct.unpack("<H", token[offset:offset + 2])[0]        if field_len:            field_offset = struct.unpack("<I", token[offset + 4:offset + 8])[0]            return token[field_offset:field_offset + field_len]def hmac_md5(key, data):    return hmac.new(key, data, digestmod=hashlib.md5).digest()def md4(m):    h = MD4.new()    h.update(m)    return h.digest()def md5(m):    return hashlib.md5(m).digest()def ntowfv1(password):    return md4(password.encode('utf-16-le'))def rc4init(k):    arc4 = algorithms.ARC4(k)    return Cipher(arc4, mode=None, backend=default_backend()).encryptor()def rc4k(k, d):    return rc4init(k).update(d)def sealkey(session_key, usage):    direction = b"client-to-server"if usage == 'initiate'else b"server-to-client"    return md5(session_key + b"session key to %s sealing key magic constant\x00" % direction)def signkey(session_key, usage):    direction = b"client-to-server"if usage == 'initiate'else b"server-to-client"    return md5(session_key + b"session key to %s signing key magic constant\x00" % direction)def get_tcp_streams(pcap_path, port=5985):    """Read pcap and reassemble TCP streams for the given port using scapy."""    pkts = rdpcap(pcap_path)    raw_streams = collections.defaultdict(list)    for p in pkts:        if TCP in p and IP in p and Raw in p:            if p[TCP].dport == port or p[TCP].sport == port:                key = (p[IP].src, p[TCP].sport, p[IP].dst, p[TCP].dport)                raw_streams[key].append((p[TCP].seq, bytes(p[Raw])))    streams = {}    for key, segs in raw_streams.items():        segs.sort(key=lambda x: x[0])        assembled = b''        last_end_seq = None        for seq, data in segs:            if last_end_seq is None:                assembled = data                last_end_seq = seq + len(data)            elif seq >= last_end_seq:                # Normal continuation or gap                assembled += data                last_end_seq = seq + len(data)            elif seq + len(data) > last_end_seq:                # Partial overlap (e.g. retransmission with new data)                overlap = last_end_seq - seq                assembled += data[overlap:]                last_end_seq = seq + len(data)            # else: fully overlapping retransmission, skip        streams[key] = assembled    return streamsdef parse_http_messages(data):    """Parse HTTP messages from a reassembled TCP stream byte string."""    messages = []    pos = 0    while pos < len(data):        header_end = data.find(b'\r\n\r\n', pos)        if header_end == -1:            break        header_section = data[pos:header_end]        lines = header_section.split(b'\r\n')        if not lines:            break        first_line = lines[0].decode('utf-8', errors='replace')        headers = {}        for line in lines[1:]:            if b':'in line:                k, v = line.split(b':', 1)                key_name = k.strip().lower().decode('utf-8', errors='replace')                headers[key_name] = v.strip().decode('utf-8', errors='replace')        body_start = header_end + 4        content_length = int(headers.get('content-length', '0').strip())        body = data[body_start:body_start + content_length]        messages.append({            'first_line': first_line,            'headers': headers,            'body': body,        })        pos = body_start + content_length    return messagesdef unpack_message(data):    """Parse a multipart/encrypted WinRM body into (length, wrapped_data) pairs."""    parts = data.split(b'--Encrypted Boundary\r\n')  # was incorrectly commented out    parts = list(filter(None, parts))    messages = []    for i in range(0, len(parts), 2):        if i + 1 >= len(parts):            break        header = parts[i].strip()        payload = parts[i + 1]        try:            length = int(header.split(b"Length=")[1])        except (IndexError, ValueError):            continue        # Remove the end MIME block if it exists        if payload.endswith(b"--Encrypted Boundary--\r\n"):            payload = payload[:len(payload) - 24]        wrapped_data = payload.replace(b"Content-Type: application/octet-stream\r\n", b"")        messages.append((length, wrapped_data))    return messagesdef pretty_xml(xml_str):    dom = xml.dom.minidom.parseString(xml_str)    return dom.toprettyxml()def main():    """Main program entry point."""    args = parse_args()    if args.password:        nt_hash = ntowfv1(args.password)    else:        nt_hash = base64.b16decode(args.hash.upper())    print(f"[*] Reading pcap: {args.path}", file=sys.stderr)    streams = get_tcp_streams(        os.path.expanduser(os.path.expandvars(args.path)),        args.port    )    print(f"[*] Found {len(streams)} TCP stream(s) on port {args.port}", file=sys.stderr)    # Identify client-side stream keys: dst_port == WinRM port    # key format: (src_ip, src_port, dst_ip, dst_port)    client_keys = [key for key in streams.keys() if key[3] == args.port]    for req_key in sorted(client_keys, key=lambda k: k[1]):        src_ip, src_port, dst_ip, dst_port = req_key        resp_key = (dst_ip, dst_port, src_ip, src_port)        if resp_key not in streams:            print(f"[!] No response stream for client port {src_port}", file=sys.stderr)            continue        print(f"\n[*] Processing session: {src_ip}:{src_port} <-> {dst_ip}:{dst_port}", file=sys.stderr)        req_msgs = parse_http_messages(streams[req_key])        resp_msgs = parse_http_messages(streams[resp_key])        print(f"[*] {len(req_msgs)} requests, {len(resp_msgs)} responses", file=sys.stderr)        context = SecurityContext(src_port, nt_hash)        for req_msg, resp_msg in zip(req_msgs, resp_msgs):            # --- Process NTLM token from request (type1 NEGOTIATE or type3 AUTHENTICATE) ---            auth_header = req_msg['headers'].get('authorization', '')            if auth_header.lower().startswith('negotiate '):                try:                    token = base64.b64decode(auth_header.split(' ', 1)[1])                    if token.startswith(b"NTLMSSP\x00"):                        context.add_token(token)                except Exception as e:                    print(f"[!] Failed to parse Authorization token: {e}", file=sys.stderr)            # --- Process NTLM token from response (type2 CHALLENGE) ---            www_auth = resp_msg['headers'].get('www-authenticate', '')            if www_auth.lower().startswith('negotiate '):                try:                    token = base64.b64decode(www_auth.split(' ', 1)[1])                    if token.startswith(b"NTLMSSP\x00"):                        context.add_token(token)                except Exception as e:                    print(f"[!] Failed to parse WWW-Authenticate token: {e}", file=sys.stderr)            if not context.complete:                continue            # --- Decrypt request body (client -> server) ---            if b'--Encrypted Boundary'in req_msg['body']:                try:                    for length, enc_data in unpack_message(req_msg['body']):                        # Trim trailing MIME padding to exact plaintext length                        enc_data = enc_data[:20 + length]                        msg = context.unwrap_initiate(enc_data)                        try:                            dec_xml = pretty_xml(msg[:length].decode('utf-8'))                        except Exception as e:                            print(f"[!] XML decode error: {e}", file=sys.stderr)                            dec_xml = repr(msg[:length])                        print(f"[Client Port {src_port}] {src_ip} -> {dst_ip}\n{dec_xml}\n")                except Exception as e:                    print(f"[!] Failed to decrypt request body: {e}", file=sys.stderr)            # --- Decrypt response body (server -> client) ---            if b'--Encrypted Boundary'in resp_msg['body']:                try:                    for length, enc_data in unpack_message(resp_msg['body']):                        enc_data = enc_data[:20 + length]                        msg = context.unwrap_accept(enc_data)                        try:                            dec_xml = pretty_xml(msg[:length].decode('utf-8'))                        except Exception as e:                            print(f"[!] XML decode error: {e}", file=sys.stderr)                            dec_xml = repr(msg[:length])                        print(f"[Client Port {src_port}] {dst_ip} -> {src_ip}\n{dec_xml}\n")                except Exception as e:                    print(f"[!] Failed to decrypt response body: {e}", file=sys.stderr)def parse_args():    """Parse and return args."""    parser = argparse.ArgumentParser(description='Parse network captures from WireShark and decrypts the WinRM '                                                 'messages that were exchanged.')    parser.add_argument('path',                        type=str,                        help='The path to the .pcap/.pcapng file to decrypt.')    parser.add_argument('--port',                        dest='port',                        default=5985,                        type=int,                        help='The port to scan for the WinRM HTTP packets (default: 5985).')    secret = parser.add_mutually_exclusive_group()    secret.add_argument('-p', '--password',                        dest='password',                        help='The password for the account that was used in the authentication.')    secret.add_argument('-n', '--hash',                        dest='hash',                        help='The NT hash for the account that was used in the authentication.')    if argcomplete:        argcomplete.autocomplete(parser)    return parser.parse_args()if __name__ == '__main__':    main()        
```  
```
#!/usr/bin/env python3# -*- coding: utf-8 -*-# PYTHON_ARGCOMPLETE_OK# Copyright: (c) 2020 Jordan Borean (@jborean93) <jborean93@gmail.com># MIT License (see LICENSE or https://opensource.org/licenses/MIT)# Fork / modifications by Haoxi Tan (haoxi.tan@gmail.com)# MIT License (see LICENSE or https://opensource.org/licenses/MIT)"""Script that can read a Wireshark capture .pcap/.pcapng for a WinRM exchange and decrypt the messages.Currently only supports exchanges that were authenticated with NTLM.Fixes applied to the original script:  1. tshark_path: passed directly to pyshark.FileCapture so Wireshark need not be in PATH.  2. unpack_message: the data.split() line was incorrectly commented out causing NameError.  3. context lookup: original used list[-1] which raises IndexError on empty list; use next() instead.  4. error handling: replaced bare re-raise with print+continue so one bad frame doesn't crash the run."""from __future__ import (absolute_import, division, print_function)__metaclass__ = typeimport argparseimport base64import hashlibimport hmacimport osimport pysharkimport binasciiimport sysimport structimport xml.dom.minidomfrom Crypto.Hash import MD4from cryptography.hazmat.primitives.ciphers import (    algorithms,    Cipher,)from cryptography.hazmat.backends import (    default_backend,)try:    import argcompleteexcept ImportError:    argcomplete = None# Path to tshark.exe — change this if Wireshark is installed elsewhere.TSHARK_PATH = r'D:\杂项工具\网络流量分析\wireshark\tshark.exe'class SecurityContext:    def __init__(self, port, nt_hash):        self.port = port        self.tokens = []        self.nt_hash = nt_hash        self.complete = False        self.key_exch = False        self.session_key = None        self.sign_key_initiate = None        self.sign_key_accept = None        self.seal_handle_initiate = None        self.seal_handle_accept = None        self.__initiate_seq_no = 0        self.__accept_seq_no = 0    @property    def _initiate_seq_no(self):        val = self.__initiate_seq_no        self.__initiate_seq_no += 1        return val    @property    def _accept_seq_no(self):        val = self.__accept_seq_no        self.__accept_seq_no += 1        return val    def add_token(self, token):        self.tokens.append(token)        if token.startswith(b"NTLMSSP\x00\x03"):            # Extract the info required to build the session key            nt_challenge = self._get_auth_field(20, token)            b_domain = self._get_auth_field(28, token) or b""            b_username = self._get_auth_field(36, token) or b""            encrypted_random_session_key = self._get_auth_field(52, token)            flags = struct.unpack("<I", token[60:64])[0]            encoding = 'utf-16-le'if flags & 0x00000001 else'windows-1252'            domain = b_domain.decode(encoding)            username = b_username.decode(encoding)            # Derive the session key            nt_proof_str = nt_challenge[:16]            response_key_nt = hmac_md5(self.nt_hash, (username.upper() + domain).encode('utf-16-le'))            key_exchange_key = hmac_md5(response_key_nt, nt_proof_str)            self.key_exch = bool(flags & 0x40000000)            if self.key_exch and (flags & (0x00000020 | 0x00000010)):                self.session_key = rc4k(key_exchange_key, encrypted_random_session_key)            else:                self.session_key = key_exchange_key            # Derive the signing and sealing keys            self.sign_key_initiate = signkey(self.session_key, 'initiate')            self.sign_key_accept = signkey(self.session_key, 'accept')            self.seal_handle_initiate = rc4init(sealkey(self.session_key, 'initiate'))            self.seal_handle_accept = rc4init(sealkey(self.session_key, 'accept'))            self.complete = True    def unwrap_initiate(self, data):        print('unwrap_initiate', file=sys.stderr)        return self._unwrap(self.seal_handle_initiate, self.sign_key_initiate, self._initiate_seq_no, data)    def unwrap_accept(self, data):        print('unwrap_accept', file=sys.stderr)        return self._unwrap(self.seal_handle_accept, self.sign_key_accept, self._accept_seq_no, data)    def _unwrap(self, handle, sign_key, seq_no, data):        header = data[4:20]        enc_data = data[20:]        dec_data = handle.update(enc_data)        b_seq_num = struct.pack("<I", seq_no)        checksum = hmac_md5(sign_key, b_seq_num + dec_data)[:8]        if self.key_exch:            checksum = handle.update(checksum)        actual_header = b"\x01\x00\x00\x00" + checksum + b_seq_num        if header != actual_header:            print("Signature verification failed", file=sys.stderr)        return dec_data    def _get_auth_field(self, offset, token):        field_len = struct.unpack("<H", token[offset:offset + 2])[0]        if field_len:            field_offset = struct.unpack("<I", token[offset + 4:offset + 8])[0]            return token[field_offset:field_offset + field_len]def hmac_md5(key, data):    return hmac.new(key, data, digestmod=hashlib.md5).digest()def md4(m):    h = MD4.new()    h.update(m)    return h.digest()def md5(m):    return hashlib.md5(m).digest()def ntowfv1(password):    return md4(password.encode('utf-16-le'))def rc4init(k):    arc4 = algorithms.ARC4(k)    return Cipher(arc4, mode=None, backend=default_backend()).encryptor()def rc4k(k, d):    return rc4init(k).update(d)def sealkey(session_key, usage):    direction = b"client-to-server"if usage == 'initiate'else b"server-to-client"    return md5(session_key + b"session key to %s sealing key magic constant\x00" % direction)def signkey(session_key, usage):    direction = b"client-to-server"if usage == 'initiate'else b"server-to-client"    return md5(session_key + b"session key to %s signing key magic constant\x00" % direction)def unpack_message(data):    # BUG FIX: the split line was incorrectly commented out in the original script,    # causing a NameError ("parts" not defined) on the very next line.    parts = data.split(b'--Encrypted Boundary\r\n')    parts = list(filter(None, parts))    messages = []    for i in range(0, len(parts), 2):        if i + 1 >= len(parts):            break        header = parts[i].strip()        payload = parts[i + 1]        try:            length = int(header.split(b"Length=")[1])        except (IndexError, ValueError):            continue        # remove the end MIME block if it exists        if payload.endswith(b"--Encrypted Boundary--\r\n"):            payload = payload[:len(payload) - 24]        wrapped_data = payload.replace(b"Content-Type: application/octet-stream\r\n", b"")        messages.append((length, wrapped_data))    return messagesdef pretty_xml(xml_str):    dom = xml.dom.minidom.parseString(xml_str)    return dom.toprettyxml()def main():    """Main program entry point."""    args = parse_args()    if args.password:        nt_hash = ntowfv1(args.password)    else:        nt_hash = base64.b16decode(args.hash.upper())    # FIX: pass tshark_path so pyshark finds tshark even when Wireshark is not in PATH.    captures = pyshark.FileCapture(        os.path.expanduser(os.path.expandvars(args.path)),        display_filter='http and tcp.port == %d' % args.port,        tshark_path=TSHARK_PATH,    )    contexts = []    forcapin captures:        try:            source_port = int(cap.tcp.srcport)            unique_port = source_port if source_port != args.port else int(cap.tcp.dstport)            auth_token = None            if hasattr(cap.http, 'authorization'):                b64_token = cap.http.authorization.split(' ')[1]                auth_token = base64.b64decode(b64_token)            elif hasattr(cap.http, 'www_authenticate'):                parts = cap.http.www_authenticate.split(' ')                if len(parts) > 1:                    b64_token = parts[1]                    try:                        auth_token = base64.b64decode(b64_token)                    except Exception:                        continue                else:                    continue            context = None            if auth_token:                if not auth_token.startswith(b"NTLMSSP\x00"):                    continue                if auth_token.startswith(b"NTLMSSP\x00\x01"):                    context = SecurityContext(unique_port, nt_hash)                    contexts.append(context)                else:                    # BUG FIX: original used [-1] which raises IndexError on an empty list.                    # Use next() with a default of None instead.                    context = next((c for c in contexts if c.port == unique_port), None)                    if not context:                        print("Missing existing NTLM security context for port %s, skipping frame %s"                              % (unique_port, cap.number), file=sys.stderr)                        continue                context.add_token(auth_token)            if hasattr(cap.http, 'file_data'):                if not context:                    context = next((c for c in contexts if c.port == unique_port), None)                if not context:                    print("No security context found for port %s, skipping frame %s"                          % (unique_port, cap.number), file=sys.stderr)                    continue                if not context.complete:                    # BUG FIX: original raised ValueError here, crashing the whole run.                    # Print a warning and skip this frame instead.                    print("Context not yet complete for port %s, skipping frame %s"                          % (unique_port, cap.number), file=sys.stderr)                    continue                length = int(cap.mime_multipart.data_len)                msgdata = binascii.unhexlify(cap.mime_multipart.data)                messages = [(length, msgdata)]                unwrap_func = context.unwrap_accept if source_port == args.port else context.unwrap_initiate                dec_msgs = []                for length, enc_data in messages:                    msg = unwrap_func(enc_data)                    try:                        dec_msgs.append(pretty_xml(msg.decode('utf-8')))                    except Exception as e:                        print("Exception: ", e, file=sys.stderr)                        dec_msgs.append("[bad message]")                dec_msgs = "\n".join(dec_msgs)                print("No: %s | Time: %s | Source: %s | Destination: %s\n%s\n"                      % (cap.number, cap.sniff_time.isoformat(), cap.ip.src_host, cap.ip.dst_host, dec_msgs))        except Exception as e:            # BUG FIX: original re-raised here, crashing on any single bad frame.            # Print the error and continue to the next frame instead.            print("Failed to process frame %s: %s" % (cap.number, e), file=sys.stderr)            continuedef parse_args():    """Parse and return args."""    parser = argparse.ArgumentParser(description='Parse network captures from WireShark and decrypts the WinRM '                                                 'messages that were exchanged.')    parser.add_argument('path',                        type=str,                        help='The path to the .pcap/.pcapng file to decrypt.')    parser.add_argument('--port',                        dest='port',                        default=5985,                        type=int,                        help='The port to scan for the WinRM HTTP packets (default: 5985).')    secret = parser.add_mutually_exclusive_group()    secret.add_argument('-p', '--password',                        dest='password',                        help='The password for the account that was used in the authentication.')    secret.add_argument('-n', '--hash',                        dest='hash',                        help='The NT hash for the account that was used in the authentication.')    if argcomplete:        argcomplete.autocomplete(parser)    return parser.parse_args()if __name__ == '__main__':    main()
```  
  
上面这两个是ai生成的winrm流量提取的脚本，第一个没有使用tshark，第二个是使用tshark的 然后也是成功提取出全部的winrm流量![](https://mmbiz.qpic.cn/sz_mmbiz_png/g673ce4c7rlK3w0vp5Cv3eUnZtZyRAayF5gScU6BcSZ7mrSCdo0kV394y0vdm6zgL1kTUvxFeicgNBLRHUpoNWeWSlqdbfUZ14TkKKwpbAt8/640?wx_fmt=png&from=appmsg "")  
![](https://mmbiz.qpic.cn/sz_mmbiz_png/g673ce4c7rkibsJcNMibbZDqJdghIrrhOlFwn1wLHPOTUFcVicLBOePKdlVOnZGpWNqhxSpskiaic2cQt8Ax1JoXFTmqp94EwcC0oL4j4J7aZLuA/640?wx_fmt=png&from=appmsg "")  
github上找到一个脚本也是提取winrm流量的： https://github.com/h4sh5/decrypt-winrm/blob/main/winrm_decrypt.py 但是我尝试过了，他只能分析一点，因为这个脚本严格按照一定结构去提取的，所以遇到结构不规范的就会报错，然后就不会往下提取了  
  
然后要对txt文件进行命令的解码  
```
import reimport base64def read_file(filename):    """读取文件，自动尝试多种编码。"""    encodings_to_try = ['utf-8-sig', 'utf-8', 'gbk', 'utf-16', 'ascii']    for enc in encodings_to_try:        try:            with open(filename, 'r', encoding=enc) as f:                return f.read()        except UnicodeDecodeError:            continue    # 如果所有编码都失败，使用忽略错误的策略    with open(filename, 'r', encoding='utf-8', errors='ignore') as f:        return f.read()def is_likely_text(data: bytes) -> bool:    """    判断字节数据是否可能是可读文本。    通过检查可打印字符（包括空格和换行）的比例来实现。    """    if not data:        return False    try:        # 统计可打印ASCII字符和常见控制字符（如\n, \r, \t）        printable_count = sum(1 for b in data if 32 <= b <= 126 or b in (9, 10, 13))        ratio = printable_count / len(data)        # 如果可打印字符比例超过70%，则认为是文本        return ratio > 0.7    except Exception:        return Falsedef decode_base64_to_text(b64_str: str) -> str:    """    尝试用多种编码将Base64字符串解码为文本。    返回解码后的字符串，如果都失败则返回None。    """    # 清理字符串中的空白字符（如换行符），但保留其Base64完整性    b64_str_clean = re.sub(r'\s', '', b64_str)    # 补全可能的填充字符（=），以确保长度是4的倍数    padding_needed = len(b64_str_clean) % 4    if padding_needed:        b64_str_clean += '=' * (4 - padding_needed)    try:        decoded_bytes = base64.b64decode(b64_str_clean, validate=True)    except Exception:        # 如果不是有效的Base64数据，则跳过        return None    # 优先尝试解码为utf-8，然后尝试gbk（处理中文）    for encoding in ['utf-8', 'gbk', 'ascii']:        try:            return decoded_bytes.decode(encoding)        except UnicodeDecodeError:            continue    return Nonedef analyze_winrm_log(filename: str):    """主分析函数。"""    raw_text = read_file(filename)    # 改进的Base64正则表达式：    # 1. `(?<![A-Za-z0-9+/=])` 和 `(?![A-Za-z0-9+/=])` 确保匹配的是独立的Base64块。    # 2. `(?:[A-Za-z0-9+/]{4})*` 匹配完整的4字符组。    # 3. `(?:[A-Za-z0-9+/]{2}==|[A-Za-z0-9+/]{3}=)?` 匹配末尾的填充。    # 4. 增加`re.MULTILINE`和`re.DOTALL`标志，确保能匹配跨行内容。    base64_pattern = re.compile(        r'(?<![A-Za-z0-9+/=])'          # 前面不是Base64字符        r'(?:[A-Za-z0-9+/]{4})*'       # 任意个完整的4字符组        r'(?:[A-Za-z0-9+/]{2}==|[A-Za-z0-9+/]{3}=)?'# 可能的填充        r'(?![A-Za-z0-9+/=])',         # 后面不是Base64字符        re.MULTILINE | re.DOTALL    )    matches = base64_pattern.findall(raw_text)    print(f"共找到 {len(matches)} 个潜在的Base64字符串。\n")    meaningful_strings = []    for i, b64_str in enumerate(matches, 1):        # 过滤掉过短的匹配项（很可能是误报）        if len(b64_str) < 8:            continue        decoded_text = decode_base64_to_text(b64_str)        if decoded_text and decoded_text.strip():            # 将解码后的文本按行分割，并过滤掉空行            lines = [line.rstrip('\r\n') for line in decoded_text.splitlines() if line.strip()]            for line in lines:                meaningful_strings.append(line)    # 输出结果，并尝试区分命令和结果    print("=" * 50)    print("提取到的有意义字符串 (已去重和排序):")    print("=" * 50)    seen = set()    for text in sorted(set(meaningful_strings), key=lambda x: meaningful_strings.index(x)): # 保持原顺序去重        if text not in seen:            seen.add(text)            # 简单启发式判断：如果字符串是单个单词或常见命令格式，可能是输入的命令            if re.match(r'^[a-zA-Z0-9_.\\\s/-]+$', text) and len(text.split()) <= 5:                print(f"[命令输入] {text}")            else:                print(f"[系统输出] {text}")    print("=" * 50)if __name__ == "__main__":    analyze_winrm_log("1.txt")
```  
  
![](https://mmbiz.qpic.cn/sz_mmbiz_png/g673ce4c7rnDIJAf1qiaVoRV1OAuRxpiaskrJCzvYsOheEueYsorAOJGvMrfDvg44t7bKPicVB8RyeurN942OiaSaic29HSC2QSjOcjVTNicsibXvU/640?wx_fmt=png&from=appmsg "")  
发现了NTLM hash，然后前面不是直接爆破NTLMSSP失败了吗，那么解密smb还有一种方法就是直接去生成smb2的session key来解密smb流量 在github找到一个小脚本： https://github.com/iamdonu/SMB3-Decryption/blob/main/randomSessionKeyNTLM.py 可以直接用 解释一下参数  
- -k session key  
  
![](https://mmbiz.qpic.cn/sz_mmbiz_png/g673ce4c7rmC10WCia1zic23CJSWG6mKpZzlTKHictLa2ibAZ3hPeFPpqnbzat2RmibUDXQz3uMB7Bl0sdHEtc5HYTRJJy9Jd2zd8IGUFDPUUrt0/640?wx_fmt=png&from=appmsg "")  
- -p NTProofstr  
  
![](https://mmbiz.qpic.cn/sz_mmbiz_png/g673ce4c7rmjYsDDaZpZjiaxsTj5fAiaVNTibkdSLbDos6iayQicF8qXib2WDnib9PSNYXibibyjjVXcfq8obYQbOvSSQVwrXU890qTluTESI2Nt22CY/640?wx_fmt=png&from=appmsg "")  
- -n NT HASH  
  
- --domain  
  
![](https://mmbiz.qpic.cn/mmbiz_png/g673ce4c7rkMwamYibx1JTl7JJ4dMUKeeEpibibAbZI0vdEMdPfBQzoOicibOQDLlBRqvm5IAlO0UkjeCiaTNQCDzFL02Faz4gXI5UOMibfV3uicBPc/640?wx_fmt=png&from=appmsg "")  
  
这里留空是因为流量里他没有域,这里要记一下sessionid  
，后面解密smb3流量要用  
  
然后运行脚本python .\randomSessionKeyNTLM.py -u administrator --domain= -n 3d83254b53697355ef7498b535e7ab29 -p 4103e8d84572fa74f220ecc20be704c1 -k 7433d4ac87cdff2d38b2e8a5840b919d  
  
![](https://mmbiz.qpic.cn/sz_mmbiz_png/g673ce4c7rlIeBaRqPq9uynwLnhY7dSBS5gQyhDwtwHIAHJYmfrfoicIlMBwC62TR8mzfNvF60vKxZTXNr9Fan0hibG68nkQmfTyNfuEhInbY/640?wx_fmt=png&from=appmsg "")  
  
接着回到wireshark里开始解密流量（注意一下协议树里的hex都是小端序，所以填的时候要倒一下）![](https://mmbiz.qpic.cn/sz_mmbiz_png/g673ce4c7rn5xbCgPJFKhBcCS43OU9uNcYqGqICaC8hNY2MH72Mnaiaiad9nyAAkia5nxK62hNPQrdxmnhRVZ6a3YF0bjaragWaQQfXsRZ4Pm8/640?wx_fmt=png&from=appmsg "")  
  
  
然后就解密了一部分smb3流量了，但是发现还有一部分是加密的，也就是以用户admin发送的那一部分smb，然后就是没找到什么有用的能够辅助解密的信息，怀疑关键信息藏在刚才解密的那一部分smb3流量里![](https://mmbiz.qpic.cn/mmbiz_png/g673ce4c7rlNxrKwtggMibALdChD7wibjib2ibKvSG7icIrDWAVzau2rNVZuEeumSuy4m3c8vlGI388n3icgRFIIH8cHengC4aT6gDCicJQONoDuXY/640?wx_fmt=png&from=appmsg "")  
![](https://mmbiz.qpic.cn/mmbiz_png/g673ce4c7rn9s6ZJ4MaMoHELgE0QLHcqdvABkA5kFqpj4QsuulSRDiabYcunsiaoYWfn36PE2ibYgPYF1s4CmMVicOPpe2CUvib9sNYeMiaPAhLJM/640?wx_fmt=png&from=appmsg "")  
只不过我直接得到了这个admin用户的密码，然后我们延续上面的思路，计算出admin对应的sessionkey，ai写了个直接生成sessionkey的脚本，将生成ntlm hash和生成sessionkey一步到位了  
```
import hmac, hashlib, structfrom Crypto.Cipher import ARC4password = "kPxQ1GT9zA9E"##这个要根据实际情况进行修改from Crypto.Hash import MD4h = MD4.new(password.encode('utf-16-le'))nt_hash = h.digest()# 或者使用方法2：定义一个支持MD4的哈希函数def md4_hash(data):    from Crypto.Hash import MD4    h = MD4.new(data)    return h.digest()nt_hash = md4_hash(password.encode('utf-16-le'))server_challenge    = bytes.fromhex("b3526269d4453b24")nt_proof_str        = bytes.fromhex("7368589eef94d340237823caa7835c29")encrypted_sess_key  = bytes.fromhex("a93341fd28b90248aa3cc3e072da4cc4")username = "admin"domain   = ""def smb3_kdf(key, label, context):      label_b   = label.encode('utf-8') + b'\x00'      context_b = context.encode('utf-8') + b'\x00'      data = struct.pack('>I', 1) + label_b + b'\x00' + context_b + struct.pack('>I', 128)      return hmac.new(key, data, hashlib.sha256).digest()[:16]user_domain = (username.upper() + domain).encode('utf-16-le')ntlmv2_hash = hmac.new(nt_hash, user_domain, hashlib.md5).digest()session_base_key = hmac.new(ntlmv2_hash, nt_proof_str, hashlib.md5).digest()session_key = ARC4.new(session_base_key).decrypt(encrypted_sess_key)enc_key = smb3_kdf(session_key, "SMB2AESCCM", "ServerIn ")dec_key = smb3_kdf(session_key, "SMB2AESCCM", "ServerOut")print(f"Session Key: {session_key.hex()}")print(f"Client-to-Server: {enc_key.hex()}")print(f"Server-to-Client: {dec_key.hex()}")
```  
  
然后找到相关的信息就可以生成session key了![](https://mmbiz.qpic.cn/mmbiz_png/g673ce4c7rl8lsYXW8OJdibLIPo8s1EsOLHMmJoLEYRLhMFrwvK7wCo6jtKoWYBxK5cPTTQ72jzLoPVWpJgSS38cdkblHg9JcM2tN2gk3VyU/640?wx_fmt=png&from=appmsg "")  
![](https://mmbiz.qpic.cn/mmbiz_png/g673ce4c7rk8KBdzeYzcmmlRITTVglmibeibVl70fWPu8sabTKsD0nSeEsnPR33NNGTGIy1hv6Z12hQJcFKRxotdwMpWv6d0jmn5MghJibMVVc/640?wx_fmt=png&from=appmsg "")  
解密后就可以看到上传的证书![](https://mmbiz.qpic.cn/mmbiz_png/g673ce4c7rmjGceaGVAOFrH2s7u4gwJMibOfqicVib7ajKSeoeZvzY7T5g3XVwYmIFhfMRuZPnckzcuxCAgnqvEJ4EicABGpMicjoibY6BYNa9iaoI/640?wx_fmt=png&from=appmsg "")  
然后导出 然后用openssl  
从证书中导出私钥就好啦（密码默认是mimikatz）  
```
openssl pkcs12 -in "C:\Users\user\Desktop\1.pfx" -nocerts -nodes -out private-key.pem -passin pass:mimikatz
```  
  
![](https://mmbiz.qpic.cn/mmbiz_png/g673ce4c7rlbxpzfBuFRviacHIvYz6mjM2RQSTfP9AOOLiaT47icxkKlVvFzygIX5mjto5y652B2S2txHrW02nm7SGxef0OrYmfEGbrDrpqicro/640?wx_fmt=png&from=appmsg "")  
![](https://mmbiz.qpic.cn/sz_mmbiz_png/g673ce4c7rkibiaJwiaF6Nj6alR0FMmYhRE2mlWMiaPodjsh2t9ibpbOvX5rYPDUvaOLdicpvYMPjBQwc2U2qUAGAAR3EZOyaeDHuqVibdJ5jkZYxc/640?wx_fmt=png&from=appmsg "")  
虽然提取的过程中有报错，但是对最终结果不影响  
  
然后回到wireshark解密2-rdp.pcapng  
![](https://mmbiz.qpic.cn/sz_mmbiz_png/g673ce4c7rljaPDhWQvZ3bZvWXYmuufWmF2XbBEfteAqEhkuialBD6bKSvdSAw8wnCia4bjOplpmnOBUQP0Ah6WqLS5EYiaTmZyvibSJpyje5bE/640?wx_fmt=png&from=appmsg "")  
  
  
接着导出PDU  
，也就是将解密后的解密后的上层流量单独导出来![](https://mmbiz.qpic.cn/sz_mmbiz_png/g673ce4c7rkTA9IuBrF7tTU3RH0zVtibneaXKEdibudS8HbrEAXx5qSs9VI8H9SLEF8YdVDuSCtnSwTJQ5sOW5UlqcGUMriccXVwpibqrqkYTm0/640?wx_fmt=png&from=appmsg "")  
  
  
导出之后保存为后缀为 pcap 的文件，放入 ubuntu 虚拟机中  
  
使用如下命令提取其中的 rdp 协议数据包，并解析鼠标和键盘的操作pyrdp-convert -o output 1.pcap  
![](https://mmbiz.qpic.cn/sz_mmbiz_png/g673ce4c7rnqBO2ycDRz2sXgBuia0sfOH1ibkRpdTHZ4IibrGwPAQuicX21KjjXvk5YVa1aZ5htumRJvvB02oMgHDh5LGIeJ6cN1vqicJmV9buvU/640?wx_fmt=png&from=appmsg "")  
然后在output目录下拿到.pyrbp文件![](https://mmbiz.qpic.cn/sz_mmbiz_png/g673ce4c7rnz2vuJyYhCTH3r0icYcBjxtGDuqRqMRogeHNmr0zERRg30dscaaUvH5EZZQ7Oibg0TULM1ky6uOBkU5pIoiaxGIDMxxTKiaQibq368/640?wx_fmt=png&from=appmsg "")  
使用pyrbp自带的播放器打开那个pyrbp文件![](https://mmbiz.qpic.cn/sz_mmbiz_png/g673ce4c7rkLDLMBYJAuYhPicHxn6OGo9dkQ7RmFOw3ZtMeBxgPv7RicAeTTu9cG9lJt3GiaHmkdQmarSbhAbo0ibe4Zhxy6RbgibwViaFZnXuTAk/640?wx_fmt=png&from=appmsg "")  
结果爆炸了，因为我的ubuntu虚拟机好像资源调度不过来了，那只能再度请出ai大人帮帮我了  
```
#!/usr/bin/env python3"""PyRDP 回放文件解析器从 1.pyrdp 文件中提取键盘和鼠标操作行为，输出人类可读的时间线。"""import structimport sysfrom io import BytesIOfrom datetime import datetime, timedeltafrom collections import defaultdict# ───────────────────────── 复用 pyrdp 库的模块 ─────────────────────────from pyrdp.core import Uint16LE, Uint64LE, Uint8, Int16LE, Uint32LEfrom pyrdp.enum import PlayerPDUType, MouseButton, scancodefrom pyrdp.enum.rdp import PointerFlag, KeyboardFlagfrom pyrdp.parser import PlayerParser, SlowPathParser, BasicFastPathParserfrom pyrdp.enum import ParserModefrom pyrdp.pdu import (    PlayerPDU, PlayerKeyboardPDU, PlayerMouseMovePDU, PlayerMouseButtonPDU, PlayerMouseWheelPDU,    FastPathScanCodeEvent, FastPathMouseEvent, FastPathUnicodeEvent,    KeyboardEvent, MouseEvent, FastPathMouseEvent,    InputPDU,    ConfirmActivePDU,)# ───────────────────────── 工具函数 ─────────────────────────def format_ts(ms: int, start_ms: int) -> str:    """将毫秒时间戳格式化为 +XX.XXX 秒格式"""    delta = (ms - start_ms) / 1000.0    return f"+{delta:9.3f}s"def get_key_name(scancode_val: int, is_extended: bool, shift_pressed: bool, caps_on: bool) -> str:    """获取按键名称（复用 pyrdp 的 scancode 映射）"""    return scancode.getKeyName(scancode_val, is_extended, shift_pressed, caps_on)def is_modifier(scancode_val: int) -> bool:    """判断是否为修饰键"""    return scancode_val in [0x1D, 0x2A, 0x36, 0x38, 0x5B, 0x5C]# ───────────────────────── 主解析器 ─────────────────────────class PyRDPParser:    def __init__(self, filepath: str):        self.filepath = filepath        self.data = open(filepath, 'rb').read()        self.events = []  # [(timestamp_ms, event_type, description), ...]        self.start_ms = 0        # 键盘状态        self.shift_pressed = False        self.caps_on = False        self.alt_pressed = False        self.ctrl_pressed = False        self.win_pressed = False        # 鼠标状态        self.mouse_pos = (0, 0)        # 连接信息        self.hostname = ""        self.username = ""        self.password = ""        self.domain = ""        # 解析器实例        self.player_parser = PlayerParser()        self.slow_path_parser = SlowPathParser()        self.fast_path_parser = BasicFastPathParser(ParserMode.SERVER)    def run(self):        print("=" * 70)        print("  PyRDP 回放文件解析器 - 键盘与鼠标行为还原")        print("=" * 70)        print(f"  文件: {self.filepath} ({len(self.data)} 字节)")        print("=" * 70)        # 第一步：解析所有事件        self._parse_all()        if not self.events:            print("\n[!] 未找到任何事件")            return        # 第二步：输出会话信息        self._print_session_info()        # 第三步：输出键盘鼠标事件时间线        self._print_timeline()        # 第四步：输出文本摘要（组合的键盘输入）        print("\n" + "=" * 70)        print("  文本输入摘要（从键盘事件还原）")        print("=" * 70)        self._print_text_summary()    def _parse_all(self):        """解析文件中的所有 PlayerPDU"""        parser = PlayerParser()        stream = BytesIO(self.data)        offset = 0        while offset < len(self.data):            # 检查是否至少有一个完整的 PDU 长度头            if offset + 8 > len(self.data):                break            # 读取 PDU 长度            length = struct.unpack('<Q', self.data[offset:offset+8])[0]            if length < 18 or offset + length > len(self.data):                # 无效记录，尝试跳过                offset += 1                continue            # 提取 PDU 数据            pdu_data = self.data[offset:offset+length]            offset += length            try:                pdu = parser.parse(pdu_data)                self._process_pdu(pdu)            except Exception as e:                print(f"  [!] PDU 解析警告: {e}", file=sys.stderr)    def _process_pdu(self, pdu: PlayerPDU):        """处理一个 PlayerPDU"""        if self.start_ms == 0:            self.start_ms = pdu.timestamp        ts = pdu.timestamp        if pdu.header == PlayerPDUType.FAST_PATH_INPUT:            self._handle_fast_path_input(ts, pdu)        elif pdu.header == PlayerPDUType.FAST_PATH_OUTPUT:            # 输出事件（图像更新等）——忽略            pass        elif pdu.header == PlayerPDUType.SLOW_PATH_PDU:            self._handle_slow_path(ts, pdu)        elif pdu.header == PlayerPDUType.CLIENT_INFO:            self._handle_client_info(pdu)        elif pdu.header == PlayerPDUType.CLIENT_DATA:            self._handle_client_data(pdu)        elif pdu.header == PlayerPDUType.CLIPBOARD_DATA:            self._handle_clipboard(ts, pdu)        elif pdu.header == PlayerPDUType.CONNECTION_CLOSE:            self.events.append((ts, "connection", "连接关闭"))        elif pdu.header == PlayerPDUType.KEYBOARD:            self._handle_player_keyboard(ts, pdu)        elif pdu.header == PlayerPDUType.MOUSE_MOVE:            mpdu = pdu  # type: PlayerMouseMovePDU            self.mouse_pos = (mpdu.x, mpdu.y)            self.events.append((ts, "mouse_move", f"鼠标移动至 ({mpdu.x}, {mpdu.y})"))        elif pdu.header == PlayerPDUType.MOUSE_BUTTON:            mpdu = pdu  # type: PlayerMouseButtonPDU            btn_name = {1: "左键", 2: "右键", 3: "中键"}.get(mpdu.button.value, f"按键{mpdu.button.value}")            action = "按下"if mpdu.pressed else"释放"            self.mouse_pos = (mpdu.x, mpdu.y)            self.events.append((ts, "mouse_btn", f"鼠标{btn_name}{action} 于 ({mpdu.x}, {mpdu.y})"))        elif pdu.header == PlayerPDUType.MOUSE_WHEEL:            mpdu = pdu  # type: PlayerMouseWheelPDU            direction = "水平"if mpdu.horizontal else"垂直"            self.events.append((ts, "mouse_wheel", f"鼠标滚轮({direction}) 偏移{mpdu.delta} 于 ({mpdu.x}, {mpdu.y})"))        elif pdu.header == PlayerPDUType.TEXT:            # 文本事件（单字符）——可以由键盘事件推断，暂时忽略            pass    # ──────── FastPath 输入（最常包含键盘鼠标）────────    def _handle_fast_path_input(self, ts: int, pdu: PlayerPDU):        """处理 FAST_PATH_INPUT - 包含键盘和鼠标事件"""        try:            fpdu = self.fast_path_parser.parse(pdu.payload)            for event in fpdu.events:                if isinstance(event, FastPathScanCodeEvent):                    self._handle_scancode(ts, event.scanCode, event.isReleased,                                          event.rawHeaderByte & 0x02 != 0)                elif isinstance(event, FastPathMouseEvent):                    self._handle_mouse_event(ts, event)                elif isinstance(event, FastPathUnicodeEvent):                    ch = event.text                    if not event.released and ch:                        self.events.append((ts, "unicode", f"字符: '{ch}' "))        except Exception as e:            pass  # 解析失败则跳过    # ──────── SlowPath 输入 ────────    def _handle_slow_path(self, ts: int, pdu: PlayerPDU):        """处理 SLOW_PATH_PDU"""        try:            spdu = self.slow_path_parser.parse(pdu.payload)            if isinstance(spdu, ConfirmActivePDU):                # 可以在这里捕获分辨率等信息                pass            elif isinstance(spdu, InputPDU):                for event in spdu.events:                    if isinstance(event, KeyboardEvent):                        flags = event.flags                        released = bool(flags & KeyboardFlag.KBDFLAGS_DOWN == 0)                        extended = bool(flags & KeyboardFlag.KBDFLAGS_EXTENDED)                        self._handle_scancode(ts, event.keyCode, released, extended)                    elif isinstance(event, MouseEvent):                        self.mouse_pos = (event.x, event.y)                        flags = event.flags                        if flags & PointerFlag.PTRFLAGS_MOVE:                            self.events.append((ts, "mouse_move", f"鼠标移动至 ({event.x}, {event.y})"))                        if flags & PointerFlag.PTRFLAGS_BUTTON1:                            action = "按下"if flags & PointerFlag.PTRFLAGS_DOWN else"释放"                            self.events.append((ts, "mouse_btn", f"鼠标左键{action} 于 ({event.x}, {event.y})"))                        if flags & PointerFlag.PTRFLAGS_BUTTON2:                            action = "按下"if flags & PointerFlag.PTRFLAGS_DOWN else"释放"                            self.events.append((ts, "mouse_btn", f"鼠标右键{action} 于 ({event.x}, {event.y})"))                        if flags & PointerFlag.PTRFLAGS_WHEEL:                            direction = "上"if not (flags & PointerFlag.PTRFLAGS_WHEEL_NEGATIVE) else"下"                            self.events.append((ts, "mouse_wheel", f"鼠标滚轮{direction} 于 ({event.x}, {event.y})"))        except Exception as e:            pass    # ──────── 键盘 Scancode 处理 ────────    def _handle_scancode(self, ts: int, scan_code: int, released: bool, extended: bool):        key_name = get_key_name(scan_code, extended, self.shift_pressed, self.caps_on)        # 更新修饰键状态        if scan_code == 0x2A or scan_code == 0x36:  # Shift            self.shift_pressed = not released        elif scan_code == 0x1D:  # Ctrl            self.ctrl_pressed = not released        elif scan_code == 0x38:  # Alt            self.alt_pressed = not released        elif scan_code == 0x5B or scan_code == 0x5C:  # Windows/Meta            self.win_pressed = not released        elif scan_code == 0x3A and not released:  # Caps Lock            self.caps_on = not self.caps_on        action = "按下"if not released else"释放"        # 获取修饰键状态描述        mods = []        if self.ctrl_pressed: mods.append("Ctrl")        if self.alt_pressed: mods.append("Alt")        if self.shift_pressed: mods.append("Shift")        if self.win_pressed: mods.append("Win")        mod_str = f"[{' + '.join(mods)}] "if mods else""        # 单字符按键（非修饰键、按下时）直接显示字符        if not released and len(key_name) == 1 and not is_modifier(scan_code):            self.events.append((ts, "key_char", f"按键 '{key_name}' ({mod_str}scan:0x{scan_code:02X})"))        else:            desc = f"{mod_str}{key_name} {action}"if key_name != f"Unknown scan code {hex(scan_code)}"else f"未知按键 (scan:0x{scan_code:02X}, ext:{extended}) {action}"            self.events.append((ts, "key_special"if not released else"key_release",                                f"{desc} (scan:0x{scan_code:02X})"))    # ──────── 鼠标事件处理 ────────    def _handle_mouse_event(self, ts: int, event: FastPathMouseEvent):        self.mouse_pos = (event.mouseX, event.mouseY)        flags = event.pointerFlags        # 鼠标移动        if flags & PointerFlag.PTRFLAGS_MOVE:            self.events.append((ts, "mouse_move", f"鼠标移动至 ({event.mouseX}, {event.mouseY})"))        # 鼠标滚轮        if flags & PointerFlag.PTRFLAGS_WHEEL:            direction = "上"if flags & PointerFlag.PTRFLAGS_WHEEL_NEGATIVE else"下"            self.events.append((ts, "mouse_wheel", f"鼠标滚轮{direction} 于 ({event.mouseX}, {event.mouseY})"))        # 鼠标按键        if flags & PointerFlag.PTRFLAGS_DOWN:            for btn_flag, btn_name in [(PointerFlag.PTRFLAGS_BUTTON1, "左键"),                                        (PointerFlag.PTRFLAGS_BUTTON2, "右键"),                                        (PointerFlag.PTRFLAGS_BUTTON3, "中键")]:                if flags & btn_flag:                    action = "按下"if flags & PointerFlag.PTRFLAGS_DOWN else"释放"                    self.events.append((ts, "mouse_btn", f"鼠标{btn_name}{action} 于 ({event.mouseX}, {event.mouseY})"))    # ──────── 会话信息处理 ────────    def _handle_client_info(self, pdu: PlayerPDU):        try:            from pyrdp.parser import ClientInfoParser            parser = ClientInfoParser()            info = parser.parse(pdu.payload)            self.username = info.username.replace("\x00", "")            self.password = info.password.replace("\x00", "")            self.domain = info.domain.replace("\x00", "")        except Exception:            pass    def _handle_client_data(self, pdu: PlayerPDU):        try:            from pyrdp.parser import ClientConnectionParser            parser = ClientConnectionParser()            data = parser.parse(pdu.payload)            self.hostname = data.coreData.clientName.strip("\x00")        except Exception:            pass    def _handle_clipboard(self, ts: int, pdu: PlayerPDU):        try:            from pyrdp.parser import ClipboardParser            from pyrdp.pdu import FormatDataResponsePDU            from pyrdp.core import decodeUTF16LE            parser = ClipboardParser()            cpdu = parser.parse(pdu.payload)            if isinstance(cpdu, FormatDataResponsePDU):                text = decodeUTF16LE(cpdu.requestedFormatData)                self.events.append((ts, "clipboard", f"剪贴板: {text[:200]}"))        except Exception:            pass    # ──────── 输出 ────────    def _print_session_info(self):        print("\n  会话信息:")        if self.hostname:            print(f"    主机名:   {self.hostname}")        if self.username:            print(f"    用户名:   {self.username}")        if self.domain:            print(f"    域名:     {self.domain}")        if self.password:            print(f"    密码:     {self.password}")    def _print_timeline(self):        """输出事件时间线"""        print("\n" + "=" * 70)        print("  操作时间线")        print("=" * 70)        if not self.events:            print("  (无事件)")            return        # 按时间排序        self.events.sort(key=lambda x: x[0])        for ts, etype, desc in self.events:            tstr = format_ts(ts, self.start_ms)            icon = {                "key_char": "[K]",                "key_special": "[K]",                "key_release": "    ",                "mouse_move": "[M]",                "mouse_btn": "[M]",                "mouse_wheel": "[M]",                "unicode": "[U]",                "clipboard": "[C]",                "connection": "[X]",            }.get(etype, "   ")            # 对于按键释放事件，只显示 scan code 简略信息            if etype == "key_release":                print(f"  {tstr}  {icon}  {desc}")            else:                print(f"  {tstr}  {icon}  {desc}")        duration = (self.events[-1][0] - self.start_ms) / 1000.0        print(f"\n  总时长: {duration:.1f} 秒 ({len(self.events)} 个事件)")    def _print_text_summary(self):        """输出组合文本摘要"""        # 按时间排序        self.events.sort(key=lambda x: x[0])        text_chars = []        for ts, etype, desc in self.events:            if etype == "key_char":                # 提取字符                if"'"in desc:                    ch = desc.split("'")[1]                    text_chars.append(ch)            elif etype == "key_special":                if"Enter"in desc:                    text_chars.append("\n")                elif"Tab"in desc:                    text_chars.append("\t")                elif"Backspace"in desc:                    if text_chars:                        text_chars.pop()                elif"Space"in desc or "空格"in desc:                    text_chars.append(" ")        result = "".join(text_chars)        if result.strip():            print(f"\n  还原的键盘输入内容:\n")            for line in result.split("\n"):                if line.strip():                    print(f"    {line}")        else:            print("  (未检测到有效的文本输入)")# ───────────────────────── 简易模式（不依赖 pyrdp）─────────────────────────def simple_parse(filepath: str):    """    纯 struct 解析模式 —— 只解析 PlayerPDU 头部，    对 FAST_PATH_INPUT 做基础的 scancode 提取。    用于备用 / 分析底层数据。    """    data = open(filepath, 'rb').read()    print(f"\n[DEBUG] 文件大小: {len(data)} 字节")    print(f"[DEBUG] 原始记录分布:\n")    offset = 0    type_counts = defaultdict(int)    while offset < len(data):        if offset + 8 > len(data):            break        length = struct.unpack('<Q', data[offset:offset+8])[0]        if length < 18 or offset + length > len(data):            offset += 1            continue        pdu_type = struct.unpack('<H', data[offset+8:offset+10])[0]        timestamp = struct.unpack('<Q', data[offset+10:offset+18])[0]        type_counts[pdu_type] += 1        offset += length    type_names = {        1: "FAST_PATH_INPUT",        2: "FAST_PATH_OUTPUT",        3: "CLIENT_INFO",        4: "SLOW_PATH_PDU",        5: "CONNECTION_CLOSE",        6: "CLIPBOARD_DATA",        7: "CLIENT_DATA",        8: "MOUSE_MOVE",        9: "MOUSE_BUTTON",        10: "MOUSE_WHEEL",        11: "KEYBOARD",        12: "TEXT",        13: "FORWARDING_STATE",        14: "BITMAP",    }    total = sum(type_counts.values())    for tid in sorted(type_counts.keys()):        name = type_names.get(tid, f"UNKNOWN_{tid}")        count = type_counts[tid]        pct = count / total * 100        print(f"    [{tid:2d}] {name:25s}: {count:5d} 条 ({pct:5.1f}%)")# ───────────────────────── 主入口 ─────────────────────────if __name__ == "__main__":    import os    # 定位文件    script_dir = os.path.dirname(os.path.abspath(__file__))    filepath = os.path.join(script_dir, "1.pyrdp")    if not os.path.exists(filepath):        print(f"[-] 找不到文件: {filepath}")        sys.exit(1)    # 先用简单模式看统计数据    simple_parse(filepath)    # 完整解析    parser = PyRDPParser(filepath)    parser.run()
```  
  
![](https://mmbiz.qpic.cn/sz_mmbiz_png/g673ce4c7rkJfhW2qYUf4IicsDySoP3BIoharMPePqhXtFsWs28HtVWu8wguRVQOjng4B4OkG3ibgftfOsjoRzS0WqevUaknt4gUk7DGx1Jao/640?wx_fmt=png&from=appmsg "")  
![](https://mmbiz.qpic.cn/mmbiz_png/g673ce4c7rnP2AhNIiaJZ4lXkMicFBHn9zqskhxYX47KUSdltf7qxFj1OS4ubA3gIAmkkem7A6gOI8St7SjIOtCqxQkEqKAKtj1TESv5XEFv0/640?wx_fmt=png&from=appmsg "")  
终于得到flag，真是绷不住了  
  
![](https://mmbiz.qpic.cn/sz_mmbiz_jpg/g673ce4c7rkEYdyUF00Wib6ajzEYL3XymXjjVOlia36iaib408NaCeHNhkhiaiac9Aibm6BWB5Ed3icZuLzpVUsNj1gWyrkmv9oK8XXNYkKyVaCIqO0/640?wx_fmt=jpeg&from=appmsg "")  
  
  
  
